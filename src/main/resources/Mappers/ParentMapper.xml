<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.me.preschoolme.parent.ParentMapper">

    <select id="selCode">
        select K.ikid, K.kidnm, K.iclass, K.code
        from t_kid K
        join t_parent P
        on K.code = P.code
        where P.code =#{code}
    </select>

    <insert id="insKid">
        insert into t_parent_kid
        set ikid = #{ikid} and iparent = #{iparent}
    </insert>

    <select id="selId">
        select uid
        from t_parent
        where uid = #{uid}
    </select>

    <update id="upCheckUid">
        <selectKey resultType="String" keyProperty="uid" order="BEFORE">
            select uid
            from t_parent
        </selectKey>
        update duplication_check
    </update>

    <insert id="insParent">
        <selectKey resultType="String" keyProperty="ikid" order="BEFORE">
            select ikid
            from t_kid
            where code = #{code}
        </selectKey>
        <selectKey resultType="String" keyProperty="iparent" order="BEFORE">
            insert into t_parent
            set parent_nm =#{parentNm},
            uid=#{uid}, upw=#{upw}, phone_nb=#{phoneNb}, irelation = #{irelation}
            irank =1
        </selectKey>
        insert into t_parent_kid
        set ikid = #{ikid},
        iparent = #{iparent}

    </insert>
    <update id="putParent">
        update t_parent
        set parent_nm = #{parentNm}, phone_nb = #{phoneNb},
        address = #{address}, upw = #{upw}

    </update>

   

</mapper>